[project]
name = "pokemon-forme-insertion"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "coloredlogs>=15.0.1",
]

[dependency-groups]
dev = [
    "coverage>=7.16.1",
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]

[tool.setuptools]
py-modules = []
